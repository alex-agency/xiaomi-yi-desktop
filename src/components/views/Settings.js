'use strict';

var React = require('react');

var Settings = React.createClass({
    render() {
        return (
            <div className="panel"></div>
        )
    }
});

module.exports = Settings;
